@model IEnumerable<CosmeticShopAPI.Models.AuditLog>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TableName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ActionType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OldData)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NewData)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TimestampMl)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TableName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ActionType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OldData)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NewData)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TimestampMl)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.IdUser)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdLog">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdLog">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdLog">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
