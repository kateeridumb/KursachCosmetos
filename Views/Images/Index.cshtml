@model IEnumerable<CosmeticShopAPI.Models.Image>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ImageUrl)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescriptionImg)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Product)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ImageUrl)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescriptionImg)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Product.IdProduct)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdImage">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdImage">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdImage">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
